export default interface Button {
  primary: {
    backgroundColor: string;
    fontColor: string;
    borderRadius: string;
  };
  secondary: {
    backgroundColor: string;
    fontColor: string;
    borderRadius: string;
  };
  tertiary: {
    backgroundColor: string;
    fontColor: string;
    borderRadius: string;
  };
  quaternary: {
    backgroundColor: string;
    fontColor: string;
    borderRadius: string;
  };
}
