export default interface Tooltip {
  error: {
    borderColor: string;
    backgroundColor: string;
    fontColor: string;
  };
  success: {
    borderColor: string;
    backgroundColor: string;
    fontColor: string;
  };
  info: {
    borderColor: string;
    backgroundColor: string;
    fontColor: string;
  };
}
