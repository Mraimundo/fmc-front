import React from 'react';

import { Container } from './styles';

const Filters: React.FC = () => {
  return (
    <Container>
      <h2>Filtros</h2>
    </Container>
  );
};

export default Filters;
